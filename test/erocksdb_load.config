{mode, max}.

{duration, 10}.

{concurrent, 64}.

{driver, basho_bench_driver_rocksdb}.

{key_generator, {int_to_bin_bigendian,{uniform_int, 1000000}}}.

{value_generator, {fixed_bin, 10000}}.

{operations, [{get, 5}, {put, 1}]}.

{code_paths, ["ebin"]}.

{erocksdb_dir, "/tmp/erocks.bench"}.
{erocksdb_num_instances, 32}.

